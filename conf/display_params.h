/* Graphics settings*/

#define DISP_WIDTH			640		//width of the screen (pixels)
#define DISP_HEIGHT 		640		//height of the screen (pixels)
#define COORD_PIXEL_RATIO	2		//how many spatial ticks per pixel
#define VISUAL_UNIT (SPACE_UNIT / COORD_PIXEL_RATIO)